#include <iostream>
#include <vector>
using namespace std;
int n, s, element;
void dfs(int level){
    if(level == n){

    }
    for (int i = 0; i < n;i++){
        
    }
}
vector<int> v;
int main(){
    cin >> n >> s;
    for (int i = 0; i < n;i++){
        cin >> element;
        v.push_back(element);
    }
    return 0;
}